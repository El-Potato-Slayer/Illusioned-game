import * as Phaser from 'phaser'

export default class GameOver extends Phaser.Scene{

  // constructor(){
  //   this.failure = null;
  //   this.
  // }

  // preload(){
  //   this.load.image('failure')
  // }
  
  // gameOverText() {
  //   let failure = this.add.image(this.cameras.main.scrollX + 425, this.cameras.main.scrollY + 250, "You're a failure")
  //             failure.alpha = 0
  //             let restart = this.add.image(this.cameras.main)

  // }
}
